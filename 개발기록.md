# KOSME 캘린더 — 개발 기록 (Development Log)

> 중소벤처기업진흥공단 부서 일정·가용인력 공유 앱
> 최초 호남연수원 단일 부서용 → 중진공 전 부서 멀티테넌트로 확장
> 기록일: 2026-05-31

---

## 1. 개요

| 항목 | 내용 |
|---|---|
| 이름 | KOSME 캘린더 |
| 목적 | 부서원 일정(휴가·출장·회의·연수·교육) 공유 + **가용 인력 파악** |
| 형태 | 설치형 모바일 웹앱(PWA), 데스크탑 호환 |
| 접속 | https://kosme2412005.github.io/honam-calendar/ |
| 저장소 | GitHub `kosme2412005/honam-calendar` (public) |
| 백엔드 | Supabase (Postgres + Auth + Realtime + RLS) |
| 소스 | `C:\Users\honamedu\honam-calendar\app.html` (배포 시 `index.html`로 복사) |

---

## 2. 개발 타임라인 (진화 과정)

### 단계 0 — 기획 & 검토
- 인터뷰로 요구사항 도출: 문제 = "결재문서는 전산망에 있으나 한눈에 보는 창구 없음 → 구두 공유 의존"
- 진짜 목적 재정의: 단순 일정공유 → **"지금 누가 활용 가능한 인력인가"** (가용 인력 가시화)
- 연수원 특수성 5가지: 연수/교육 운영(종일 몰입)·장소 분산(내부↔외부 호텔)·출장 3종(회의/외부행사/영업)·연초 1년치 계획·5명 미만 폐강
- **적대적 검토(critic) 3회** → 치명 결함(데이터 유실 등) 발견·해소 → `기획서_최종.md`(v4)

### 단계 1 — 사내망 자체서버 버전 (초기, 현재는 대체됨)
- 사무실 PC가 외부 클라우드를 차단 → **순수 Node.js + SSE 사내망 서버**로 시작
- 파일: `server.js`, `서버켜기.bat`, `index.html`(localStorage 프로토타입)

### 단계 2 — 클라우드 전환 (핵심 피벗)
- 요구 변경: **모바일이 주 사용 + 출장 중(외부)에서도 접속 필요**
- 통찰: "외부 클라우드 차단"은 *사무실 PC(업무망)* 정책이고, **개인 휴대폰(LTE)은 그 필터 밖** → 외부 접속 가능한 서버가 정답
- 결정: **Supabase(클라우드) + 정적 PWA(GitHub Pages)**, 실제 로그인, 행수준 보안(RLS)

### 단계 3 — 앱 개발 & 모바일 배포
- 단일 파일 PWA(`app.html`): 로그인 · 월간 캘린더 · **가용 인력 뷰** · 상태(예정/확정/폐강) · 장소 · 출장 세부유형 · CSV 일괄등록 · 실시간 동기화
- GitHub Pages 배포 → 폰에서 접속·"홈 화면 추가"로 앱처럼 사용

### 단계 4 — PWA 설치 + 권한
- 매니페스트·서비스워커·아이콘 → "앱 설치" 버튼(한 번 탭 설치)
- 권한: **본인 일정만 편집**, 방 관리자 전체

### 단계 5 — 멀티테넌트(부서별 방)
- 카톡 오픈톡방 방식: 방 만들기(부서명+비번) → 입장 → 부서별 독립 캘린더
- **서버 강제 격리(RLS)** — 다른 부서 데이터 원천 차단
- 기존 데이터는 '호남연수원' 방으로 이전

### 단계 6 — 마무리 다듬기
- 브랜딩: 로고 "중진공", 제목 "KOSME 캘린더"
- 헤더/탭 레이아웃 정리, 부서명 마크, 가용 로직(무이력=가용)
- **분류 커스터마이즈** → 방(DB) 공유 방식(관리자 설정, 부서원 공유)
- 회원가입 이메일 확인 OFF(동료 합류 간소화)

### 단계 7 — 방 삭제(전체 관리자 전용)
- 방 목록 화면에 "－ 방 삭제" 토글 버튼 추가(컨트롤러 계정에만 노출)
- 삭제 모드 → 방 목록이 빨갛게 변하고, 방 클릭 시 삭제
- **실수 방지 2단 확인**: ⚠ 경고 → 방 이름 직접 타이핑해야 삭제 진행
- 서버 강제: `delete_room` RPC는 `is_controller()`만 통과, 그 외 거부
- 방 삭제 시 그 방의 부서원·일정도 함께 삭제(`ON DELETE CASCADE`)
- SQL: `supabase_delete_room_upgrade.sql`

### 단계 8 — 실사용 피드백 반영(버그 수정)
- **캘린더 토요일 칸 잘림** — 이벤트 칩 `word-break:keep-all`(한글 단어 안 끊김) 때문에 좁은 칸에서 긴 한글이 칸을 넘어가도 줄바꿈 안 돼 맨 오른쪽(토요일)이 카드 `overflow:hidden` 경계에 잘림 → `overflow-wrap:anywhere`로 변경, 모바일 칸/칩 여백 축소로 글자 공간 확보
- **타인 일정 작성자 이름 오표시** — `fillMembers`가 비관리자에게 드롭다운을 본인만 채워, 관리자가 입력한 일정을 열람해도 작성자가 본인 이름으로 보임(오해 소지) → 일정의 **실제 작성자(소유자)** 를 고정 표시하도록 수정. 관리자(컨트롤러+방장) 제외 모든 일반 부서원에 적용

### 단계 9 — 일정 색상 체계 개편(분류색 → 담당자색)
- **문제**: CSV로 올린 연수과정이 대부분 분류 "연수운영"(주황) → 캘린더가 전부 노란색이라 과정 구별 불가
- **해결**: 일정 칩 색상 기준을 **분류 → 담당자**로 변경
  - 담당자별 색: 신원·조류림=빨강(`#dc2626`), 박지용=검정(`#111827`), 박세진=초록(`#16a34a`), 정경민=핑크(`#ec4899`), 양동호=파랑(`#2563eb`), 그 외=회색(`#64748b`) — `MEMBER_COLORS` 이름 매핑
- **분류 표기 변경**: 색을 빼고 **검은 글자 뱃지(맑은 고딕, 굵게)** 로 표시 — 칩 색과 무관하게 읽히도록 흰 배경 작은 뱃지(`.cat-tag`) 위 검은 글씨
- **일관성 정리**: 범례를 "담당자 색상 안내"로 교체 / 분류 필터칩 색 제거+맑은 고딕 / 담당자 필터칩은 켜면 담당자 색 / 리스트 보기도 동일(막대=담당자색, 분류=검은 뱃지)
- 변경 함수: `memberColor()` 신설, `makeChip`·`renderList`·`renderLegend`·`renderFilters` 수정 (`app.html`)

### 단계 10 — 색상 직접편집·UI 간소화·일자 상세 팝업
- **담당자 색 직접 선택**: [부서원] 관리 화면에서 이름 옆 색버튼 → 팔레트(검정·회색 + 무지개 12색, 총 14색)에서 선택 → `members.color`에 저장. `memberColor()`가 `members.color` 우선 사용(미지정 시 이름 기본색→회색). 색 편집은 방 관리자 전용(관리 화면이 관리자 전용, RLS `members_upd`도 방 관리자만 허용)
  - SQL: `supabase_member_color_upgrade.sql` (`alter table members add column color text`)
- **출장 세부유형 제거**: 일정 추가/수정 모달에서 해당 항목·관련 JS 삭제(`sub_type`은 `null`로 저장)
- **분류 톱니(⚙) 위치 이동**: 분류 칩 뒤 → "분류" 라벨 바로 우측(`renderFilters`에서 gear append 제거, HTML에서 라벨 옆 배치)
- **캘린더 칩 간소화**: 분류 + 담당자 + 제목만 표시(시간·장소 제거)
- **일자 상세 팝업(`openDay`)**: 일자/일정 클릭 → 그날 일정 목록을 카드형 팝업(`dayOverlay`)으로 표시(담당자 색·분류·시간·장소·메모·상태) → 일정 클릭 시 수정 화면, 하단 "이 날짜에 일정 추가" 버튼. 흐름: 캘린더(간단) → 일자 클릭(상세) → 일정 클릭(수정)

### 단계 11 — 권한 개방·복수 담당자·달력 레이아웃 정리
- **색상 편집 전체 개방**: 색 변경을 방 관리자뿐 아니라 **같은 방 부서원 전원**에게 개방. 직접 UPDATE(이름·권한 보호) 대신 `set_member_color(member_id,color)` RPC(SECURITY DEFINER, `is_room_member` 통과 시만)로 처리. [부서원] 버튼을 전원에게 노출(추가·삭제 버튼·입력란은 관리자만, 비관리자에겐 안내문구)
- **일정에 부서원 복수 지정**: `events.member_ids uuid[]` 추가, 대표=`member_id`(첫 번째, 색·소유·가용인력 기준). 모달 부서원 선택을 단일 `<select>` → 다중 칩 토글(`renderEvtMembers`/`pickedMembers`). 칩·리스트·팝업·필터(`passFilter`)·가용인력(`dayStatus`)·이름표시(`evMembers`/`evMemberNames`) 모두 복수 반영
- **일정 수정 전체 개방**: 일반 부서원도 **남이 등록한 일정을 수정**(예: 본인 이름 추가) 가능하도록 `events_upd` RLS를 `is_room_member`로 개방. 단 **삭제는 작성자 본인/관리자만**(`events_del` 유지, 삭제 버튼도 조건부 노출). 신규 등록은 본인이 대표(INSERT 정책 유지 → 저장 시 본인을 맨 앞으로 정렬)
- **달력 레이아웃**: 일·토 칸을 1/2 폭으로 축소(`grid-template-columns:0.5fr 1fr×5 0.5fr`), 월~금 확대 / 칩 표시를 **분류 → 부서원(각 한 줄) → 제목** 순서로 각각 줄바꿈(`.cat-tag`·`.evt-mem`·`.evt-title` block) / 제목은 한 단계 작게(0.85em)
- SQL: `supabase_member_color_upgrade.sql`(단계10), `supabase_multi_member_and_open_color.sql`, `supabase_event_update_open.sql`

### 단계 12 — 설치(PWA) 장벽 완화
- **문제**: 부서원이 카톡으로 받은 링크를 누르면 **카톡 인앱 브라우저**로 열려 설치 버튼이 안 뜨고, 아이폰은 수동 단계라 기종마다 설치법이 달라 진입장벽이 큼
- **상황 자동감지 안내 배너(`updateInstallGuide`/`envInfo`)**: 접속환경(UA·standalone)을 감지해 맞춤 안내 하나만 화면 **최상단**에 자동 표시
  - 카톡/네이버 등 **인앱 브라우저** → "기본 브라우저로 열기"(안드로이드 카톡은 `kakaotalk://web/openExternal`로 자동 외부 열기, 아이폰은 Safari로 열기 안내)
  - **안드로이드 일반 브라우저** → `beforeinstallprompt` 캡처해 **"앱 설치" 원터치 버튼**
  - **아이폰 Safari** → "공유 → 홈 화면에 추가" 2단계 안내
  - 이미 설치(standalone)·캘린더 사용 중·× 닫음(localStorage) → 숨김
- **배너 위치 수정**: 최초 로그인 화면(`authView`)에도 보이도록 배너를 뷰 바깥 **문서 최상단**으로 이동(이전엔 방 화면에만 있어 미로그인 카톡 사용자에게 안 보였음) → 흐름: **설치 먼저 → 앱에서 회원가입 → 이후 앱만 실행**
- **배포 보조물**: `qr.png`(앱 주소 QR — 카메라 스캔 시 인앱 우회), `install.html`(그림 단계별 설치 안내 페이지, 배포), `설치안내.md`(부서원 공유용 텍스트)
- 참고: 아이폰 자동설치는 애플 정책상 불가 → 안내가 최선. 데스크탑은 배너 숨김

### 단계 13 — 세션 유지 버그 수정(방 안에 있다가 ~30분 뒤 튕김)
- **증상**: 호남연수원 방에 입장해 일정표를 보다가 **약 30분 뒤 자동으로 부서 공간 선택 화면으로 되돌아감** (오류사항2 → 오류사항1)
- **원인**: Supabase는 로그인 토큰을 주기적으로(약 30분) 자동 갱신하는데, 이때 `onAuthStateChange` 콜백이 다시 호출됨. 기존 코드는 세션만 있으면 **이벤트 종류를 가리지 않고 무조건 `showRoomView()`**(방 선택 화면) 실행 → 토큰 갱신·앱 포커스 복귀마다 방 밖으로 튕겨 나감
- **수정(`app.html` `onAuthStateChange`)**: 세션이 있어도 **이미 방 안(`currentRoomId`)·앱 화면·방 선택 화면에 있으면 `return`**으로 화면을 유지. 새 로그인(앱/방 화면이 떠 있지 않은 상태)일 때만 `showRoomView()` 이동. 로그아웃(세션 없음) 시엔 `currentRoomId` 초기화 후 로그인 화면으로
- 효과: 토큰이 갱신돼도 일정표 화면 그대로 유지 → 오래 켜둬도 튕기지 않음

---

## 3. 아키텍처

```
[부서원 휴대폰/PC]
   │  https (LTE/사내WiFi 어디서나)
   ▼
[GitHub Pages]  ── 정적 PWA(index.html) 제공
   │  supabase-js
   ▼
[Supabase]  Postgres + Auth(로그인) + Realtime(실시간) + RLS(방 격리)
```

- **인증**: Supabase Auth(이메일+비번), 가입 시 이름·부서명 저장
- **멀티테넌트**: `rooms`(방) · `members`(방별 부서원) · `events`(방별 일정), 모두 `room_id`로 분리
- **격리**: RLS + 헬퍼 함수(`is_room_member`/`is_room_admin`/`is_controller`) + RPC(`create_room`/`join_room`/`list_rooms`/`my_rooms`/`delete_room`/`set_member_color`)
- **권한 정책(현행)**: 색상=같은 방 부서원 전원 변경 / 일정 **수정**=같은 방 부서원 전원 / 일정 **추가**=본인(또는 관리자) / 일정 **삭제**=작성자 본인 또는 관리자 / 부서원 추가·삭제=방 관리자
- **일정 담당자**: `events.member_ids`(배열, 복수) + 대표 `member_id`(첫 번째 — 색·소유·가용인력 기준)
- **비밀번호**: pgcrypto 해시 저장, 서버 검증(평문 노출 없음)
- **분류**: `rooms.categories`(jsonb), RPC(`get/set_room_categories`), 방 관리자만 설정
- **권한**: 일정은 본인 것만(서버 RLS), 방 관리자=방 전체, 컨트롤러(`kosme2412005@gmail.com`)=슈퍼관리자

---

## 4. 파일 구성 (`C:\Users\honamedu\honam-calendar\`)

| 파일 | 설명 | 상태 |
|---|---|---|
| `app.html` | **현재 앱 소스** (배포본 원본) | 사용 중 |
| `index.html` | 초기 localStorage 프로토타입 | 참고용 |
| `server.js` · `서버켜기.bat` | 사내망 Node 서버 버전 | 대체됨 |
| `supabase_schema.sql` | 초기 테이블/RLS | 실행됨 |
| `supabase_rooms_upgrade.sql` | 멀티테넌트(방) | 실행됨 |
| `supabase_categories_upgrade.sql` | 분류 방-공유 | 실행됨 |
| `supabase_delete_room_upgrade.sql` | 방 삭제(관리자 전용) | 실행됨 |
| `supabase_member_color_upgrade.sql` | 담당자 색상 컬럼(`members.color`) | 실행됨 |
| `supabase_multi_member_and_open_color.sql` | 일정 복수 담당자(`events.member_ids`)·색상 개방 RPC(`set_member_color`) | 실행됨 |
| `supabase_event_update_open.sql` | 일정 수정 권한 전체 개방(`events_upd` RLS) | 실행됨 |
| `manifest.json` · `sw.js` · `icon-*.png` | PWA 설치 | 배포됨 |
| `install.html` · `qr.png` · `설치안내.md` | 설치 안내 페이지·QR·텍스트 안내 | 배포됨 |
| `기획서_최종.md` | 기획서(검토 3회 반영, v4) | — |
| `보안_개인정보_질의서.md` | 전산/보안 질의서 초안 | — |
| `클라우드_시범_설정가이드.md` | Supabase 설정 가이드 | — |
| `실행안내.md` | (사내망 버전) 실행 안내 | 구버전 |
| `개발기록.md` | 이 문서 | — |

> 배포: `app.html` → `honam-deploy/index.html`로 복사 후 GitHub에 push (별도 클론 `C:\Users\honamedu\honam-deploy`).

---

## 5. 주요 설정값 / 접속 정보

| 항목 | 값 |
|---|---|
| 앱 주소 | https://kosme2412005.github.io/honam-calendar/ |
| Supabase URL | https://shbhyaoelamnmmyrwecb.supabase.co |
| Supabase anon key | 앱에 내장(공개용 — 안전) |
| 컨트롤러(슈퍼관리자) | kosme2412005@gmail.com |
| '호남연수원' 방 임시 비번 | (관리자 별도 보관) |
| GitHub 저장소 | kosme2412005/honam-calendar (public) |
| 회원가입 이메일 확인 | OFF(시범 편의) |

---

## 6. 현재 상태 & 남은 일

### ✅ 완료
- 기획·설계검토 · 클라우드 셋업 · 앱 개발 · 모바일 배포
- 멀티테넌트(부서별 방) · 서버 격리(RLS) · PWA 설치
- 분류 방-공유 · 회원가입 간소화

### ⏳ 남은 일 (가드레일 / 운영)
1. **부서원 동의** — 외부 클라우드 저장 고지·동의
2. **민감정보 자제** — 메모란에 병가 사유 등 미기재
3. **시범 운영** — 2~3명 1주 → 피드백 (Go/No-Go)
4. **정식 운영 시** — 국내/공공 클라우드 이전 + 전산/보안 검토

### 💡 향후 기능 후보
알림, 통계(출장유형·가동률), 반복 일정, 분류 변경 실시간 반영, 권한 세분화

---

## 7. 배포 방법 (기억용)
```bash
# app.html 수정 후
cp app.html ../honam-deploy/index.html
cd ../honam-deploy
git add index.html && git commit -m "..."
git push <PAT-URL> HEAD:main
# GitHub Pages가 1~2분 후 자동 반영
```
> DB 구조 변경이 필요하면 `supabase_*.sql`을 Supabase SQL Editor에서 실행.
